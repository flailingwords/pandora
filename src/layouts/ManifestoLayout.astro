---
import ArticleLayout from '@/layouts/ArticleLayout.astro'

const { chapter, headings, sectionsTitle } = Astro.props
---

<ArticleLayout
    article={chapter}
    headings={headings}
    sectionsTitle={sectionsTitle}
>
    <slot />
</ArticleLayout>
